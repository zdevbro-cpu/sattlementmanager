# deps
node_modules/
**/node_modules/

# env / secrets
.env
.env.local
.env.*.local
server/.env
web/.env
web/.env.local

# build output
dist/
web/dist/

# 로컬 백업 파일 (작업 안전용, 저장소 제외)
*.backup
*.backup[0-9]*
*.bk-*
*.bak_*

# 잔재 프론트 환경파일
.env.production
web/.env.production

# uploads (Drive 로컬 폴백)
server/uploads/

# 데이터 백업 (주민번호·계좌 등 개인정보 포함 — 저장소 제외)
backups/

# firebase 배포 캐시
.firebase/

# logs
*.log
npm-debug.log*

# editor / os
.DS_Store
Thumbs.db
